name: dart_consume_chat
description: A simple command-line application.

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  dio: ^3.0.9
  socket_io_client: ^0.9.10
  socket_io: ^0.9.4
  web_socket_channel: ^1.1.0

dev_dependencies:
  pedantic: ^1.9.0

